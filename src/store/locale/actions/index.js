export default {
  changeLanguage(context, obj) {
    context.commit('change', obj);
  },
  // push (context) {
  //   context.commit('push')
  // },
  // remove(context, params) {
  //   context.commit('remove', params)
  // },
  // pop(context) {
  //   context.commit('pop')
  // }
};
